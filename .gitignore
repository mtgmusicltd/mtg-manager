# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Electron build output — all of it is regenerated by `pnpm electron:build:mac`.
# The updater manifests live here too: they are generated artefacts, not source.
# Published manifest content belongs in the Release Log, not in this repository.
dist-electron/
